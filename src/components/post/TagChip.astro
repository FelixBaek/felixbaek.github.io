---
interface Props {
  label: string;
  href?: string;
  tone?: 'lang' | 'plain';
  count?: number;
}

const { label, href, tone = 'plain', count } = Astro.props;
const toneClass =
  tone === 'lang'
    ? 'chip chip--lang'
    : 'chip';
---

{
  href ? (
    <a class={toneClass} href={href}>
      {label}
      {typeof count === 'number' && <span class="ml-1 tabular-nums text-fg-caption">{count}</span>}
    </a>
  ) : (
    <span class={toneClass}>
      {label}
      {typeof count === 'number' && <span class="ml-1 tabular-nums text-fg-caption">{count}</span>}
    </span>
  )
}
